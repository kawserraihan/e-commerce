export type CategoryType = {
  id: number; // Ensure this matches the API response
  category_name: string; // This matches the API response
  is_active: boolean; // This should be a boolean as per the API response
};