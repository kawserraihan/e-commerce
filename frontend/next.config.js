/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['demoapi.anticbyte.com', 'ecstasybd.com', 'www.msmart.shop'], 
  },
};

module.exports = nextConfig;
